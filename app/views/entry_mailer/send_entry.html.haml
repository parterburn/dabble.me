!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %p Just reply to this email with your entry for #{Time.now.strftime("%B %-d")}.
    - if @user.send_past_entry? && @random_entry
      %p
        %b Oh snap, remember this? #{@random_entry.time_ago_in_words_or_numbers} ago you wrote...
      %p
        %div
          = @random_entry.body.html_safe
    - elsif @user.send_past_entry?
      %p
        %b Send your first entry and you'll see a blast from the past show up here!
    %p
      =link_to "Past entries", "https://#{ENV['MAIN_DOMAIN']}/entries"
      &middot;
      =link_to "Unsubscribe", "https://#{ENV['MAIN_DOMAIN']}/users/edit"
    %hr{:style=>"margin-top: 20px;margin-bottom: 20px;border: 0;border-top: 2px solid whiteSmoke;"}
    %p{style: "color:#CCC"}
      %i=@random_inspiration.body